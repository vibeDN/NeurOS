# NeurOS agent orchestrator config.

# Name shown in the top pane.
AGENT_NAME="Claude"

# Command run inside the tmux session. v1 target: the real Claude Code CLI
# (once it's in the image + authenticated). Until then, a mock.
#AGENT_CMD="claude"
AGENT_CMD="/usr/lib/neuros/mock-agent"

TMUX_SESSION="agent"
