# tty1 runs the compositor session (see /etc/profile.d/neuros-session.sh); if it
# crashes we want fast retries, not a start-limit lockout.
[Unit]
StartLimitIntervalSec=0

[Service]
Restart=always
RestartSec=1
