# tty1 runs the compositor session (see /etc/profile.d/neuros-session.sh).
# Restart it if the compositor exits, but don't hammer on a hard crash loop.
[Unit]
StartLimitIntervalSec=30
StartLimitBurst=6

[Service]
Restart=always
RestartSec=2
